---
const { title, isPrimary, isSecondary } = Astro.props;
---

<button class:list={{ primaryBtn: isPrimary, secondaryBtn: isSecondary }}>
  {title}
</button>

<style>
  .primaryBtn {
    padding: 12px 24px;
    border: 0;
    border-radius: 8px;
    background: var(--primaryColor);
    color: var(--white);
    font-size: 18px;
    font-weight: 600;
    margin-block: 10px;
  }
  .secondaryBtn {
    padding: 12px 24px;
    border-radius: 8px;
    border: 1px solid #82868c;
    color: #82868c;
    font-size: 18px;
    font-weight: 600;
    margin-block: 10px;
  }
</style>
